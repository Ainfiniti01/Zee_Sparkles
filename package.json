{
  "dependencies": {
    "react-icons": "^5.5.0"
  },
  "scripts": {
    "typecheck": "react-router typegen && tsc --noEmit",
    "build": "vite build",
    "predeploy": "mkdir dist && npm run build",
    "dev": "vite",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^7.1.3"
  }
}
